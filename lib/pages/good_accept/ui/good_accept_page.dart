import 'package:flutter/material.dart';

class GoodsAcceptPage extends StatelessWidget {
  const GoodsAcceptPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('GOODS ACCEPT'),
    );
  }
}